import { GrandeDirective } from './grande.directive';

describe('GrandeDirective', () => {
  it('should create an instance', () => {
    const directive = new GrandeDirective();
    expect(directive).toBeTruthy();
  });
});
